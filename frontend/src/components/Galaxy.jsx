// Galaxy component temporarily disabled due to ogl dependency conflict with React 19
// This component will be re-enabled when ogl supports React 19 or an alternative is found

export default function Galaxy() {
  return null
}
