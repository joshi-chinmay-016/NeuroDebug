// DEPRECATED: Firebase test suite is obsolete following PostgreSQL migration.
export async function testFirebaseConnection() {
  return { success: true, message: 'Firebase obsolete; PostgreSQL is active persistence layer.' };
}
